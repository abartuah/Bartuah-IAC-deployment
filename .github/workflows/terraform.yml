name: Terraform Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy Terraform
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: 1.0.9  # Set your desired Terraform version

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v3
        with:
        
          aws-secret-access-key: AKIA2VNYJOEVQQ4OL5N7
          aws-access-key-id: 9AiMFlSTOJcK7omMSg4JkObCN9QV3AuJWTb/y/Bn
          mask-aws-account-id: "no"
          aws-region: us-east-1  # Replace with your AWS region

      - name: Terraform Init
        run: terraform init

      - name: Terraform Validate
        run: terraform validate

      - name: Terraform Plan
        run: terraform plan -out=tfplan

      - name: Terraform Apply
        run: terraform apply -input=false tfplan
        # env:
        #   TF_VAR_example_var: example_value  # Set any required environment variables

      # Optionally, you can include steps to clean up or perform other actions after deployment
